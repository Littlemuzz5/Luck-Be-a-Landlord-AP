V140: Removes client commands /resync, /writemod, /hint_symbol, /check, /pckpath, /setpckpath, /senddeath, and /testdeath. /state no longer prints the old lock-everything message. Removed the old manual /senddeath V15 local DeathLink helper path. The generated mod still refreshes automatically on connect/state updates, and the PCK patch is still installed automatically when the client starts. Patch marker is AP_LIVE_POOL_PATCH_V140.
