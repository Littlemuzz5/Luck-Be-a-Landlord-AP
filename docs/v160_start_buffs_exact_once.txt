V160: fixes start buffs applying too many times. The generated AP floor no longer also adds the buff effects; Main.tscn applies each persistent start buff exactly once per run using a run key. Also improves reroll/removal/essence token buffs by updating popup tokens and Extra Sum display. Choice of Symbols now gives exactly one Big Symbol Bomb per received copy per run. Patch marker AP_LIVE_POOL_PATCH_V160.
