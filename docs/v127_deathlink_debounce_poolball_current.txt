V127: Debounces outgoing failed-payment DeathLink so the PCK bridge file and run-log fallback cannot send twice for one game over. Pool Ball no longer stays available just because Unlock: Pool Ball was received; it is active only while the current tracker/display still has Send: Pool Ball open. This means after sending Pool Ball it drops out and the next current AP item can appear. Patch marker is AP_LIVE_POOL_PATCH_V127.
