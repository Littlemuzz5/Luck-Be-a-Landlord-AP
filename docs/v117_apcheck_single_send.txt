V117: Fixes AP Check counting as two checks. The PCK check writers now write checks_to_send.json to only one watched path instead of both user:// and LOCALAPPDATA. The client also de-dupes mirrored checks_to_send payloads by nonce, skips already-sent/pending locations when choosing AP_CHECK_NEXT, and suppresses run-log AP Check detection for the same direct AP_CHECK_NEXT event. Patch marker is AP_LIVE_POOL_PATCH_V117.
